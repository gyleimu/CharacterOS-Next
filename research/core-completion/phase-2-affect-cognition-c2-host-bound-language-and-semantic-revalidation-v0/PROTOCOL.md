# AFFECT_COGNITION_C2_HOST_BOUND_LANGUAGE_AND_SEMANTIC_REVALIDATION_V0

This run tests the complete production path after Family C2. Facts remain constrained by external evidence; persistent subject state may affect only fact-compatible subjective choices.

The protocol freezes 13 Affect-absent qualification scenarios at five replicates each. Any failure stops the formal 17 × 4 × 7 matrix. If qualification passes, the formal matrix uses exact P `(0.60, 0.50)`, N `(-0.60, 0.50)`, Z `(0.00, 0.50)`, and absent-Affect conditions with a rotated round-robin schedule. No semantic/schema retries, no repair call, and no LLM judge are permitted.

The observable endpoint is the final production-delivered behavior. Scoring separately checks proposal facts, selected cognition intent, final factual correctness, final choice, contradictions, question repetition, unsupported premises, false clarification, protocol admissibility, request isolation, and Language V4 leakage.

Model-visible runtime identifiers are random opaque IDs frozen before calls and reused across P/N/Z/A for the corresponding scenario/replicate group. Research labels stay outside model input.

Maximum real calls after freeze: 551 Cognition and 551 Language. Qualification is 65, formal is 476 only after 65/65 qualification, and lawful restore confirmation is 10. Infrastructure retries are frozen at zero.


/**
 * CANONICAL_AFFECT_COMMUNICATION_DIRECTIVE_CAUSAL_EXPERIMENT_V0 helpers —
 * re-exported single-source (never reimplemented).
 */

export {
  check, canonicalJson, equal, hashJson, round
} from "../canonical-affect-behavior-influence-v1/fixtures.ts";

/**
 * PRODUCTION_SHAPED_APPRAISAL_DYNAMICS_E2 — shared deterministic helpers.
 * Single-sourced from the frozen E1 experiment helpers (never reimplemented).
 */

export {
  check, canonicalJson, equal, sha256, round12, eventPayloadHash
} from "../affect-state-retention-e1/fixtures.ts";

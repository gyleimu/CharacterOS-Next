/**
 * CANONICAL_AFFECT_COMMUNICATION_DIRECTIVE_CAUSAL_EXPERIMENT_V0 — deterministic
 * helpers (single-sourced from the frozen behavior-influence-v1 experiment
 * helpers; never reimplemented semantics).
 */

export {
  check, canonicalJson, equal, sha256, hashJson, round
} from "../canonical-affect-behavior-influence-v1/fixtures.ts";

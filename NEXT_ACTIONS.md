# NEXT_ACTIONS.md — Immediate execution boundary

Status: ACTIVE
Authority: 只定义眼前执行边界；仓库能力与成熟度以 [`CURRENT_STATE.md`](CURRENT_STATE.md) 为准。
Last verified against commit: `d4503cc6fd3f93d9e88b39d7aa365c8ef456b441`
Purpose: 指定 current baseline、blocker、next exact slice、禁止项与升级条件。

## CURRENT BASELINE

CharacterOS-Next 已有可复用 runtime、14 个 workspace、当前测试与大量冻结实验/诊断证据。旧的 pre-implementation / empty-research 状态文案已失效，不得再作为执行前提。

当前进行中的唯一 slice：

`REPOSITORY_GOVERNANCE_AND_ENGINEERING_REALITY_ALIGNMENT_V0`

其范围是 active governance/evaluation 文档、工程门禁、dependency-boundary enforcement、root command truth、lint 与最小 CI。不得修改冻结心理/语义架构或原始科学证据。

## CURRENT BLOCKER

最新 `LONG_HORIZON_AUTONOMOUS_SUBJECT_SESSION_V0` 真实运行：

- completed interactions：6/8；
- durable Memory commits：6；
- authoritative restores：2/2 `EXACT`；
- interaction 7：deterministically reproduced truncated/invalid cognition-provider JSON；
- principal result：FAILED；Memory continuity 与 autonomous orchestration：PARTIAL。

当前技术 blocker 是累计 session context 下的 provider output budget/validity，不是缺少 SubjectState、Memory、runtime 或 session capability。

## NEXT EXACT SLICE

治理 alignment 全部 gates 通过、commit/push 完成且 worktree clean 后，只恢复：

`SUBJECT_SESSION_PROVIDER_OUTPUT_BUDGET_DIAGNOSTIC_V0`

该诊断在当前治理 slice 内保持暂停。本文件不授权提前运行它。

## DO NOT START

- `INTERACTIVE_SUBJECT_PRODUCT_RUNTIME_V0` 或 autonomous-session feature expansion；
- 新 dynamics、named-emotion、Mood、ActionTendency、PersonalityScore、NarrativeSelf 或 learned-psychology 机制；
- 新 Memory schema、retrieval semantics 或 persistence/restore authority；
- 对 frozen experiment source、raw output、result、report 或 evidence 的改写；
- 为填空而创建 baseline、longitudinal、ablation、regression fixture；
- bulk legacy migration、deployment、release automation 或 license 选择；
- 把 6/8 partial validation、test volume 或同模型评价包装成通用长期主体证明。

## ESCALATION CONDITIONS

停止并请求裁定，仅当：

1. 当前 executable behavior 与仍然有效的 frozen semantic contract 无法同时成立；
2. 修复工程门禁必须改变 Affect、Appraisal、Memory、Retrieval、Cognition、Language、Behavior、Belief、Relationship、Persistence、Restore 或 Session semantics；
3. frozen raw evidence 必须被修改才能让门禁通过；
4. provider diagnostic 需要改变实验问题、模型、prompt、retrieval 或调用预算，而不是单纯测量输出截断；
5. license、外部发布、付费 provider 或其他用户/法律决策成为必要前提。

普通 stale documentation、lint debt、CI 缺失或命名冲突不构成语义升级理由。

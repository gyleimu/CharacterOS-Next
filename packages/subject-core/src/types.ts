/** P2.0 location marker only; not a runtime schema or stable contract. */
export type SubjectState = unknown;

/** P2.0 location marker only; no commit behavior is implemented. */
export type CommitRequest = unknown;

/** P2.0 location marker only; no commit behavior is implemented. */
export type CommitResult = unknown;

/** P2.0 location marker only; revision semantics remain in the frozen specs. */
export type Revision = unknown;

/** P2.0 location marker only; trace fields are not defined here. */
export type TraceEntry = unknown;

/** P2.0 location marker only; no hash projection or algorithm is implemented. */
export type StateHash = unknown;

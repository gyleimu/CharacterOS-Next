/** P2.0 location marker only; no affect production behavior exists. */
export type AffectProducer = unknown;

/** P2.0 location marker only; no affect delta schema is implemented. */
export type AffectDelta = unknown;

/** P2.0 location marker only; no mood delta schema is implemented. */
export type MoodDelta = unknown;

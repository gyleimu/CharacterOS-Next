/** P2.0 location marker only; no transition method or behavior exists. */
export type TransitionRequest = unknown;

/** P2.0 location marker only; no transition method or behavior exists. */
export type TransitionResult = unknown;

/** P2.0 location marker only; no execution context schema exists. */
export type TransitionExecutionContext = unknown;

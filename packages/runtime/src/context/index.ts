/** P2.0 location marker only; no projection construction exists. */
export type ControlledContextProjection = unknown;

/** P2.0 location marker only; no context producer or delta schema exists. */
export type ContextDelta = unknown;

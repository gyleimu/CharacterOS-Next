/** P2.0 location marker only; no regulation production behavior exists. */
export type RegulationProducer = unknown;

/** P2.0 location marker only; no regulation delta schema is implemented. */
export type RegulationDelta = unknown;

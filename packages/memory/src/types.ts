/** P2.0 location marker only; no repository methods or persistence exist. */
export type MemoryRepository = unknown;

/** P2.0 location marker only; retrieval fields are not defined here. */
export type RetrievalQuery = unknown;

/** P2.0 location marker only; retrieval behavior is not implemented. */
export type RetrievalResult = unknown;

/** P2.0 location marker only; no episodic record schema is implemented. */
export type EpisodicMemoryRecord = unknown;

/** P2.0 location marker only; no interpretation schema or provider exists. */
export type InterpretationProposal = unknown;

/** P2.0 location marker only; no provider method or LLM integration exists. */
export type InterpretationProvider = unknown;

/** P2.0 location marker only; no appraisal schema or behavior exists. */
export type AppraisalProposal = unknown;

/** P2.0 location marker only; no provider method or LLM integration exists. */
export type AppraisalProvider = unknown;
